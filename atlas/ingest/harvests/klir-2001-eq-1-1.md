# Parts harvest — klir-2001-eq-1-1

MDU until reviewed. For each ACCEPTED item: add the block to the
primitive vocabulary in atlas-core.ttl (drop the [MDU] tag), and add
`prim:<slug>` to entry:klir-2001-eq-1-1's atlas:invokesPrimitive.

## S → prim:system  (new)
- location: ch. 1, p. 4 (immediately preceding eq. (1.1))
- author's words: > It follows from this common-sense definition that the term "system" stands, in general, for a set of some things and a relation among the things.

```turtle
prim:system a skos:Concept ;
    skos:inScheme atlas:PrimitiveScheme ;
    skos:prefLabel "system"@en ;
    skos:scopeNote """Mobus's S: "It follows from this common-sense definition that the term "system" stands, in general, for a set of some things and a relation among the things." (ch. 1, p. 4 (immediately preceding eq. (1.1))). [MDU — harvested, unchecked; role proposal: unassigned]"""@en .
```

## T → prim:set-of-things  (new)
- location: ch. 2, §2.1, p. 9
- author's words: > Symbol T may stand for a single set with arbitrary elements, finite or infinite, but can also represent, for example, a power set (the set of all subsets of another set), any subset of the power set, or an arbitrary family of distinct sets.

```turtle
prim:set-of-things a skos:Concept ;
    skos:inScheme atlas:PrimitiveScheme ;
    skos:broader role:sort ;
    skos:prefLabel "set of things"@en ;
    skos:scopeNote """Mobus's T: "Symbol T may stand for a single set with arbitrary elements, finite or infinite, but can also represent, for example, a power set (the set of all subsets of another set), any subse" (ch. 2, §2.1, p. 9). [MDU — harvested, unchecked; role proposal: sort]"""@en .
```

## R → prim:relation  (REUSE)
- location: ch. 2, §2.1, p. 10
- author's words: > Cartesian products provide a basis for defining relations. A relation, in general, is a subset of some Cartesian product of given sets.
